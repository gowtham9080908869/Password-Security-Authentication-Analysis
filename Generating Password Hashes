Generating Password Hashes

import hashlib, bcrypt

# MD5
print(hashlib.md5(b"password123").hexdigest())

# SHA-1
print(hashlib.sha1(b"password123").hexdigest())

# bcrypt
hashed = bcrypt.hashpw(b"password123", bcrypt.gensalt())
print(hashed)
